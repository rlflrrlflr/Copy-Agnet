# ☁ 내 구글 드라이브 자동 백업 — 설정 안내 (팀 배포용)

## 이게 뭔가요?
소재냥인에서 **저장하는 모든 제작물(PNG)을 내 구글 드라이브에도 자동으로 쌓아주는 기능**입니다.
- 설정 안 해도 소재냥인은 정상 동작합니다. (선택 기능)
- 켜두면: PC를 바꾸거나 브라우저를 지워도 **결과물이 내 드라이브에 남습니다.**
- 내 드라이브 안 `소재냥인 제작물` 폴더가 자동으로 만들어지고 거기에 쌓입니다.

## 왜 "URL을 붙여넣으라"고 하나요?
소재냥인은 **서버가 없는 HTML 파일 하나**입니다. 그래서 여러분의 드라이브에 접근할 방법이 없습니다.
대신 **구글이 무료로 주는 개인 자동화 도구(Apps Script)**로 "내 드라이브에 파일을 넣어주는 나만의 주소"를 1회 만들고, 그 주소를 소재냥인에 알려주는 방식입니다.
→ **파일은 구글에서 내 드라이브로 바로 갑니다. 회사 서버나 외부를 거치지 않습니다.**

---

## 설정 방법 (1회, 약 3분)

소재냥인 4단계 하단의 **[☁ Drive 백업 설정]** 버튼을 누르면 아래 과정이 화면에 그대로 안내됩니다.

### ① 코드 복사
설정 창의 **[📋 코드 복사]** 버튼을 누릅니다. (아래 코드와 동일)

```javascript
function doPost(e) {
  var body = JSON.parse(e.postData.contents);
  var name = body.name || 'soszae.png';
  var folders = DriveApp.getFoldersByName('소재냥인 제작물');
  var folder = folders.hasNext() ? folders.next() : DriveApp.createFolder('소재냥인 제작물');
  var parts = String(body.data).split(',');
  var meta = parts[0];
  var mime = meta.substring(meta.indexOf(':') + 1, meta.indexOf(';'));
  var blob = Utilities.newBlob(Utilities.base64Decode(parts[1]), mime, name);
  folder.createFile(blob);
  return ContentService.createTextOutput(JSON.stringify({ ok: true }))
    .setMimeType(ContentService.MimeType.JSON);
}
```

### ② 구글 Apps Script에 붙여넣고 배포
1. **script.google.com** 접속 → 새 프로젝트가 열립니다
2. 기존 내용을 **모두 지우고** 복사한 코드를 붙여넣기 → 💾 **저장**
3. 오른쪽 위 **[배포] → [새 배포]**
4. 왼쪽 ⚙️ → **유형 선택 → 웹 앱**
5. **실행 주체: 나** / **액세스 권한: 모든 사용자** → **[배포]**
6. **[액세스 승인]** → 본인 계정 선택 → `고급` → `(안전하지 않음) 이동` → **[허용]**
   - ※ 이 경고는 "구글이 검수하지 않은 내 개인 스크립트"라는 뜻입니다. 내가 방금 만든 코드이므로 정상입니다.
7. 마지막에 나오는 **웹 앱 URL** 복사 (`https://script.google.com/macros/s/…/exec`)

### ③ 소재냥인에 붙여넣기
1. 설정 창 ③번 칸에 URL 붙여넣기
2. **[🧪 연결 테스트]** 클릭 → 내 드라이브 `소재냥인 제작물` 폴더에 `연결테스트_소재냥인.png`가 생기면 성공
3. **[저장하고 켜기]** 클릭 → 버튼이 `☁ Drive 백업 ON`으로 바뀌면 완료

---

## ⚠️ 보안 주의
- 이 URL은 **내 드라이브에 파일을 올릴 수 있는 주소**입니다. **팀 채팅·문서에 공유하지 마세요.**
- 각자 본인 것을 만들어 쓰면 됩니다 (3분이면 됩니다).
- 소재냥인은 이 URL을 **내 브라우저에만** 저장합니다. 어디로도 전송하지 않습니다.

## 자주 묻는 질문
**Q. 안 해도 되나요?** — 네. 선택 기능이고, 안 켜도 다운로드는 정상 동작합니다.
**Q. 테스트 파일이 안 생겨요.** — ①액세스 권한을 `모든 사용자`로 배포했는지 ②URL이 `/exec`로 끝나는지 ③배포 후 승인(⑥단계)을 마쳤는지 확인하세요. 수정했다면 **[배포] → [배포 관리] → 편집(연필) → 버전: 새 버전 → 배포**로 다시 배포해야 반영됩니다.
**Q. 회사 계정으로 되나요?** — 조직 정책에 따라 외부 액세스가 막혀 있을 수 있습니다. 그 경우 개인 계정으로 만들거나 IT에 문의하세요.
**Q. 끄고 싶어요.** — 설정 창에서 **[해제하기]**를 누르면 됩니다.

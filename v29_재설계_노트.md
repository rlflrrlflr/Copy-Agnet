# 소재냥인 Studio v29 — 구조 전면 재설계 노트

> 별도 파일(`v29_studio.html`). 기존 `v28_copy_engine.html`은 그대로 보존.
> 관점: 마케팅 자동화 툴 실무자 — 사용성 · 간소화 · 빈도 기반 노출 · 3단계 자유도 · 4단계 의도 반영.

## 왜 새로 썼나
v28은 기능은 완성도 높지만 **같은 함수가 8~10번 재정의(오버레이 누적)**, 916KB·6.7k행 중 상당수가 죽은 과거 버전, CDN 의존(오프라인/검증 불가)이었다. "구조의 근간을 다 건드려" 달라는 요청에 맞춰 **깨끗한 단일 파일로 재설계**.

## 근간을 바꾼 4가지
1. **함수 1정의 원칙** — 오버레이 누적 제거. 각 기능은 한 번만 정의.
2. **단일 상태(`App`)** — brief/analysis/copies/doc/final을 하나의 객체로. 렌더는 항상 상태에서 파생(`S3.renderAll()`).
3. **레이어 모델(`Doc`)** — 배너를 레이어 배열로 표현(`{type,role,nx,ny,size,font,color,align,runs,z,...}`). 정규화 좌표(0~1)로 비율 변경에 강함.
4. **무의존(CDN 0)** — 자체 CSS 디자인 시스템 + 캔버스 렌더. 오프라인 동작 → 헤드리스로 전 단계 검증 가능.

## 단계별 설계
- **1단계 브리프**: Lite/Pro 토글(필수 2개 vs 정밀). Pro 옵션은 `<details>`로 2차 접기(빈도 낮음 → 귀퉁이).
- **2단계 카피**: 3안 카드(톤·메인·서브·CTA·"왜 이 안?"). 근거(페인·톤)는 접어둠. 고르면 자동으로 기획 문서 생성 → 3단계.
- **3단계 기획(에디터·캔바식)**:
  - 레이어 클릭 **선택**, **드래그 이동**, 모서리 **크기**(SE=폰트 스케일, E=가로폭/리플로우), **더블클릭 인라인 수정**.
  - **스냅 가이드**(가로/세로 중앙·좌측 마진), **키보드 넛지**(←→↑↓, Shift=크게), **Del 삭제 / Ctrl+D 복제**.
  - **레이어 패널**(z순·표시토글), **부분색 runs**, 비율(1:1/4:5/16:9), 배경 업로드/생성.
  - **빈도 티어 속성**: T1 상시(텍스트·색·크기) / T2 접기(글꼴·정렬·CTA모양) / T3 맥락(기획 의도).
- **4단계 완성(의도 위에서)**:
  - 3단계 문서에서 **`🧭 읽어낸 기획자 의도`**(퍼널·톤·레이아웃·메인 의도·이미지 분담·무드·뱃지·수동 배치)를 명시 표로 노출.
  - 그 근거로 완성 후보 3안 생성 → 선택. **3단계 레이어 배치를 그대로 존중**해 합성(의도 이탈 X), 완성 단계에서 스크림·뱃지 칩 등 가독/완성도만 보강.
  - 고른 안에만 **피드백 반영 다듬기** + PNG 저장.

## AI 연동
- `Engine.gen(kind,payload)` 한 곳으로 통일. API Key 입력 시 `App.live=true` → `Engine._gemini`(연동 지점, 현재 stub) 호출, 실패/미입력 시 `_sim` 폴백. 실제 Gemini 프롬프트는 v28 자산을 이식하면 됨(이식 지점 명확).

## 검증(헤드리스, CDN 없이)
- 인라인 스크립트 `new Function` 구문 검사 통과.
- 전 단계 구동(1→2→3→4): 에러 0, 문서 3레이어·완성 3안 생성 확인.
- 에디터 인터랙션 단위 테스트 **전부 통과**: 넛지/추가/복제/삭제/히트테스트/인라인편집/비율리사이즈/부분색.

## v28 대비 의도적으로 단순화(또는 보류)한 것
- 고양이 로딩 게임·서류 와르르: v29엔 미이식(로딩 연출은 별도). 필요 시 컴포넌트로 분리 이식.
- 실제 Gemini 프롬프트 체인(SP_*), 파일/URL 파싱, 멀티모달: 훅만 두고 시뮬로 대체. 프로덕션화 시 v28에서 발췌 이식.
- 로고 누끼·이미지 키잉 등 고급 합성: 후속.

## v29.1 추가 (실제 모델 연동 + Claude + 캔바 근접 + 로딩 고양이)
- **모델 연동(실호출)**: `Engine`을 provider 라우팅으로 확장.
  - **Claude(카피·분석)**: 단일 HTML이라 SDK 없이 `fetch`로 `api.anthropic.com/v1/messages` 직접 호출. 헤더 `x-api-key` + `anthropic-version:2023-06-01` + `anthropic-dangerous-direct-browser-access:true`. 모델 `claude-opus-4-8`, `output_config.format`(json_schema)로 구조화 출력. (이미지 생성은 Claude 미지원 → Gemini 담당)
  - **Gemini(카피 대안 + 이미지)**: `generateContent`(텍스트는 responseSchema JSON, 이미지는 `responseModalities:["IMAGE"]` → inlineData base64).
  - 헤더에 **provider 선택(Claude/Gemini) + 두 키 입력**. 키 없으면 시뮬레이터 자동 폴백.
- **캔바 근접 인터랙션**: 레이어 **회전**(렌더 회전 + 회전 핸들 + 역회전 히트테스트 + 45° 스냅), **캔버스 정렬 6종**(좌/가운데/우/위/중앙/아래) 버튼.
- **로딩 고양이 이식**: 모든 async 생성 동안 빼꼼 고양이 오버레이(진행바 + 가위바위보 미니게임, v28 표정 에셋 재사용). `Engine.gen`이 `Cat.show/hide`로 일괄 래핑.
- **검수**: 구문 OK + 헤드리스 — provider/키 UI·CAT 에셋 로드·오버레이 표시/숨김·회전(설정/히트/핸들)·정렬(가운데/아래)·RPS 전부 통과, 에러 0.

## v29.2 — 4단계 = 디자이너 패스 (마케터 → 디자이너 핸드오프 재정의)
**문제**: 3단계에서 "그냥 얹어놓은" 거친 뱃지·폰트·배치가 4단계로 그대로 갔음(저퀄).
**해결**: 4단계가 3단계의 **콘텐츠+의도만** 입력으로 받고, **배치/폰트는 의도로만** 써서 디자이너 관점으로 재구성(`Design` 모듈).
- **타이포 위계**: 키커(아이브로우) → 디스플레이 헤드라인(워드랩·그림자) → 악센트 언더라인 → 서브 → 악센트 CTA(화살표). 그리드/세이프영역 기준 자동 배치.
- **뱃지 디자인 승격**: 3단계 평면 뱃지를 그림자·링·정돈 배치로 재렌더(우상단 디자인 규칙).
- **악센트 시스템**: CTA 색(또는 아키타입 기본)을 키커·언더라인·CTA·장식·뱃지링에 일관 적용. 명도 기반 글자색 자동.
- **장식**: 아키타입별 반짝이/헤어라인 프레임/틱(절제).
- **디자인 아키타입 5종**: 임팩트 세일 / 클린 프리미엄(명조 serif·센터) / 가격 강조 / 타임 특가(네온) / 팝 신상(Jua). 디스플레이 폰트 점진 로드(오프라인 폴백).
- **브리프별 다양성**: `Design.pickArchs(퍼널·가격뱃지)`로 3개 아키타입을 캠페인에 맞게 선택 → 브리프마다 다른 디자인 세트.
- **검수**: 구문 OK + 헤드리스 — 5종 렌더·퍼널별 선택 분기·3→4 뱃지 전달·아키타입 차별화 전부 통과, 에러 0. (스크린샷으로 CD 자기리뷰 반복)

## v29.3 — 마케터 셀프서브 + 디자이너 핸드오프 (목표: "마케터가 쓰기 편한 소재 툴")
**관점**: ①퍼포먼스 마케터가 직접 셋팅 ②때로는 실제 디자이너에게 파일을 넘겨 수정.
- **디자이너 핸드오프 내보내기**: 텍스트 없는 **배경만 PNG** / **편집 가능 SVG**(텍스트가 `<text>` 벡터 — Figma·AI에서 폰트·색·위치 그대로 편집) / **디자인 스펙(.txt)** / **전 비율 PNG(1:1·4:5·16:9)** / **3단계로 환원해 직접 편집**.
  - 이를 위해 `Design`을 **op-list 단일소스**로 리팩터 → 마케터 PNG와 디자이너 SVG가 100% 일치(로고·뱃지·텍스트·장식 포함).
- **마케터 셀프서브**: **브랜드 킷**(악센트·로고 한 번 셋팅 → 모든 완성 디자인 자동 반영) + **4단계 마케터 조정**(아키타입 스왑·악센트·배경 다시) — 디자이너 없이 직접 완성.
- **다지면 대응**: 단일 디자인을 1:1/4:5/16:9로 자동 리플로우·일괄 PNG(퍼포먼스 캠페인 다지면).
- **편의**: 🎲 예시로 채우기, **자동 저장/복원**(localStorage, 새로고침·재방문 유지), 🔄 새로.
- **검증(헤드리스, 반복마다 CD 자기리뷰)**: SVG 편집텍스트·브랜드 악센트/로고 반영·아키타입/악센트 컨트롤·전 비율 치수·샘플/저장/복원 전부 통과, 에러 0.

## v29.4 — 상품컷·레퍼런스 + 팀 공통 시트 로깅 + 드라이브 보관
- **상품컷·레퍼런스 업로드(1단계+3단계)**: `App.assets{products,refs}` + `Assets` 모듈. 상품컷=핑크 테두리/상품 태그(디테일컷 여러 장), 레퍼런스=참고. 3단계 썸네일 **클릭=배경 / Shift+클릭=이미지 레이어**(`Doc.image`, 리사이즈/회전).
  - **상품 동일성 무조건 유지**: 레이어=원본 픽셀 보존 / AI 생성=`[PRODUCT — IDENTITY LOCKED]`(모양·라벨·글자·색·비율 동일, 다시 그리거나 변형 금지) + 상품·디테일·레퍼 이미지를 **멀티모달 파트**로 전달, 레퍼런스는 `[STYLE REFERENCE]`(스타일만).
- **팀 공통 시트 로깅 이식**: 원본 툴과 **동일 Apps Script 훅**(`AKfyc…/exec`)에 `no-cors` POST. `Log` 모듈, 시트 `pref`, 형식 `[시간·광고주·단계·액션·KEY·SUB·피드백]`, 광고주 자동추정. 호출부=카피 생성(pair)·선택(select)·디자인 완성(generate). `ce19hook`/`ce19logoff` localStorage 호환.
- **드라이브 보관**: 팀 공통 폴더에 변경이력/재설계 노트/사용성 평가 업로드(버전 아카이브 연속성).
- **검증**: 업로드/썸네일/배경·레이어/동일성 락 프롬프트/멀티모달 파트수 + 로깅 훅 전송·형식·시트명 전부 통과, 에러 0.

## 남은 것(후속 — 라이브 키/대형 스코프, 한계효용 낮음)
1. **Claude 아트디렉션**(라이브): Claude가 브리프 보고 아키타입·악센트·강조어 선택(현재 퍼널 매핑).
2. **장식 스티커 라이브러리**(반짝이/리본/태양/쿠폰 드래그 레이어), **다중 선택**.
3. **배경 프롬프트 정교화**(Gemini 배경을 아키타입 팔레트 + "헤드라인/뱃지 자리 비움"), 폰트 오프라인 임베드.

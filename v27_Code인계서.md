# 소재냥인 작업 인계서 (v27 → Code)

> v27에서 Cowork 직접 처리 범위(브리프 개편·4단계 잘림·디자인모드 제거·버튼·중복뱃지·베리에이션 원복·Asset 갤러리·베리 개별재생성)를 **완료·검증**했다. 아래 3건은 **텍스트 렌더 아키텍처 변경 / 창의 반복 / 라이브 시각검수 필수** 성격이라 Cowork에서 밀다 깨지면 손실이 커서 정밀(Code) 경로로 분리한다. (프로젝트 규칙 §4 인계 기준에 정확히 해당)

## [공통 — 현재 상태]
- 파일: `v27_copy_engine.html` (단일 HTML, OneDrive `카피 엔진 제작` 폴더). 약 752KB / 6,560여 줄.
- 구조 규칙: 오버라이드 레이어(뒤 정의가 이김), 인라인 본체 = 파일 끝 마지막 `<script>` 블록. 같은 함수 다중 정의 → **마지막 정의 수정**.
- 검증 루틴: 인라인 본체 추출 → `node --check` + jsdom 스모크(document.fonts/getContext/lucide/tailwind 스텁). OneDrive Edit EPERM 가능 → bash+python 치환 후 cp가 안전.
- v27에서 막 고친 동작 회귀 금지: 브리프 스키마/렌더(SP_BRIEF18·S_BRIEF18·_brief18Render), SP_RESEARCH19 소비자보이스, 베리에이션 재생성(_ce19VarPrompt/_ce19VarPaint/_ce19VarOne/ce19MakeSizes), _drawFitNoCrop, 뱃지 미주입(updateVisualData), 완성본 버튼 위치, Asset 갤러리(_assetPush/_assetRender).

---

## 1. 고양이 벡터 재설계 ★최우선 (사용자 지정)
- **목표**: 로딩 고양이를 **벡터(SVG)** 캐릭터로 재설계 — **손(앞발) 움직임 + 표정 변화**가 가위바위보 연출과 맞물리게. 참고 SVG는 레퍼런스였을 뿐, **임베드 PNG 직접사용 지양**.
- **현재**:
  - HTML: `catStage`(~532) 안에 **거대한 base64 PNG**(~538줄, #catChar 이미지)로 캐릭터를 그림. CSS `cat-char`(in/bob 클래스로 등장·둥실 애니).
  - 게임 손은 **이모지**: `_EMO`(~6384) = {rock:'✊',scissors:'✌️',paper:'🖐'} — 고양이 '앞발'이 아니라 이모지 패가 뜬다.
  - 상태머신: `_catStory`(~6350) = 카피 작성(서류+✏️)→고양이 등장→가위바위보 제안→`catRPS(p)`(~6377) 3·2·1 카운트다운(3초 미선택=고양이 승)→승/패/무 모션(승=시무룩 슬라이드다운, 패/시간초과=서류 와르르, 무=재대결).
- **해야 할 일**:
  1. #catChar를 **인라인 SVG 고양이**(귀여운 회색 타비냥 유지)로 교체. id/클래스(`cat-char`,`in`,`bob`) 보존 → 등장·둥실 애니 그대로 동작.
  2. **앞발 포즈 3종**(rock=주먹/paper=쫙편 발/scissors=가위)을 SVG 파츠로 만들고, `catRPS`의 패 공개 시점에 이모지(_EMO) 대신 **고양이 앞발 포즈**로 전환. 카운트다운 중 발 흔들기(살짝 bob) 모션.
  3. **표정 3종**: 기본/이김=시무룩/짐=신남(또는 약올림). 승패 분기에서 얼굴 파츠 class 토글.
- **건드리지 말 것**: `_catStory`/`catRPS` 상태 흐름·타이밍·3초 룰·서류 와르르 연출. 네트워크 에러 고양이 얼럿(_alertErr).
- **완료 기준**: PNG 제거(또는 미사용), 가위바위보에서 고양이 앞발이 실제로 바뀌고 승/패/무 표정이 달라짐. 4시나리오(승·패·무·시간초과) + 무대 시각검수.
- **주의**: 귀여움 확보에 디자인 반복이 필요 → 라이브 렌더 보며 다듬는 작업. (Cowork에서 한 번에 무리하게 만들면 기존 귀여운 PNG보다 퇴보 위험 → 분리)

## 2. 보류1 — 텍스트 부분 색상(드래그 구간색) (v24 인계서 유지)
- **목표**: 메인/서브에서 **드래그로 선택한 글자 구간만** 색 지정(현재는 메인/서브 전체색 + 첫 단어 강조까지).
- **리스크**: 캔버스 직접그리기(`drawComposeText`, 활성 마지막 정의)라 run(런) 모델·인라인 편집기(_ce17*) 연동 필요 → 줄바꿈/정렬 깨질 위험(아키텍처 변경).
- **방향**: 텍스트를 run 배열(문자/구간별 color)로 모델링 → drawComposeText가 run 단위 fillText, 인라인 편집기도 run 보존. 충분한 렌더 검수.

## 3. 보류2 — 메인/서브/CTA 완전 독립 드래그 (v24 인계서 유지)
- **목표**: 세 요소를 각각 독립 위치 이동(현재는 텍스트 블록 통째 + CTA 하단바만 독립).
- **리스크**: `_cTextBox`+`_cTextParts` 기반 → 파트별 위치는 드래그 상태·히트테스트·리사이즈·정렬·완성본 렌더까지 여러 함수 동시 수정(광범위 리팩토링).
- **방향**: `composeState.pos.{key,sub,cta}` 도입, drawComposeText 파트별 위치 사용, _composeSelect/드래그/positionOverlay 파트별 박스. v24의 `_cTextParts.cta` 히트영역을 발판으로 확장.

---

## [권장 순서]
1. 고양이 벡터(독립·창의 반복) — 단독 사이클.
2. 보류1·보류2 = 텍스트 렌더 아키텍처 한 묶음(run 모델 + 파트별 위치) — 렌더 검수 필수.

## [완료 기준(전체)]
- 각 항목 완료 기준 + 기존 동선(브리프·카피 발산/선별·3·4단계·베리에이션·Asset) 회귀 0.
- 검증: 인라인 본체 node --check + jsdom 스모크 + 핵심 화면 시각검수.
- 새 버전은 vN_copy_engine.html + 변경이력.md 갱신.

# 서버 배포 노트 (테크팀용)

## 1. 기본 배포 — 정적 서빙이면 끝
- `v32_studio.html`(현행) 하나가 앱 전체(무CDN·무빌드). nginx/S3/사내 정적 호스팅 어디든 그대로 서빙.
- **HTTPS 필수**(클립보드 API·secure context 기능들). 폰트는 온라인 로드라 사내망에서 외부 폰트 CDN 허용 필요.
- 주의: localStorage는 오리진 단위 — file:// 로 쓰던 세션은 서버 URL로 안 넘어옵니다(새 시작). 키도 저장 안 하므로 재입력.
- 버전 롤백: 같은 위치에 `v31_studio.html`, `v30_studio.html`도 올려두면 URL만 바꿔 즉시 롤백.

## 2. GPT(gpt-image-2) 지원 — 프록시 1개만 부탁
OpenAI API는 브라우저 직접 호출(CORS)이 막혀 있어, 서버에 리버스 프록시가 필요합니다:
```nginx
location /openai/ {
  proxy_pass https://api.openai.com/;
  proxy_set_header Authorization "Bearer ${OPENAI_API_KEY}";  # 서버 env로 주입 — 키가 브라우저에 안 내려감(보안상 권장)
  proxy_set_header Host api.openai.com;
  add_header Access-Control-Allow-Origin $http_origin always;
  add_header Access-Control-Allow-Headers "content-type,authorization" always;
  add_header Access-Control-Allow-Methods "POST,OPTIONS" always;
  if ($request_method = OPTIONS) { return 204; }
  client_max_body_size 32m;   # images/edits 멀티파트(이미지+마스크)
  proxy_read_timeout 180s;
}
```
- 접근 제한: 사내망/VPN 한정 권장(키 대납 프록시이므로).
- 툴 연동은 `Engine._geminiImageOnce`에 provider 분기 추가로 완료 예정 — 프록시 URL만 정해지면 반영(설정칸으로 뺌).

## 3. 지속 테스트 환경 (이미 리포에 구축됨)
- **CI**: `.github/workflows/ci.yml` — 푸시·매일 03시 KST에 `_test_*.js` 전체(60여 종) 헤드리스 회귀. 로컬/서버에선 `bash run_tests.sh`.
- **A/B 벤치**: `bench/README.md` 참조 — cron 예시:
  `0 9 * * * cd /srv/sozaenyang && GEMINI_API_KEY=.. OPENAI_API_KEY=.. node bench/bench.mjs`
  `bench/out/`을 정적 서빙에 포함하면 팀이 리포트 링크로 품질 추이 확인.

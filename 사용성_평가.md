# 소재냥인 사용성 평가 & 간소화 플랜 (실무자 관점)

작성: 2026-06-17 · 대상: v28_copy_engine.html · 관점: 마케팅 자동화 툴 PdM/CD

---

## 0. 한 줄 진단
기능 완성도는 매우 높다(브리프→카피→기획→완성 4단계가 실제로 돈다). **문제는 "기능이 적어서"가 아니라 "한 화면에 다 펼쳐져서"** — 자주 쓰는 것과 1번 쓰는 것이 같은 시각적 비중으로 노출돼 인지 부하가 크다. 핵심 작업은 **노출 우선순위 정리 + 중복 제거**이고, 구조 재작성은 위험 대비 실익이 낮다.

---

## 1. 이번에 실행한 안전 작업 (검증 완료)
> 원칙: ID·핸들러 보존, 회귀 0. `node --check` + jsdom 스모크 + 단위 호출 검증 통과.

- **[3단계 패널] 저빈도 타이포 접기**: 글꼴(메인/서브) · 정렬 · CTA 모양을 `🅰 글꼴·정렬·CTA 모양 (자주 안 바꾸는 설정)` 한 줄로 접음(`<details>`). 고빈도(비율·글자색·카피·**글자 크기**)는 그대로 상단 노출. → "낮은 빈도 기능은 귀퉁이에" 요청 직접 반영.
- **[3단계 이미지 생성] 기획자 의도 반영**: `_ce17Build` 프롬프트에 `[Planner intent]` 블록 추가 — 기획 패널의 **레이아웃 아키타입**(product-shot/app-ui/persona/data-visual)을 장면 지시로, **뱃지 의도**(badgeCarries)를 "이 문구는 나중에 얹으니 자리만 비워두고 이미지 안엔 글자 넣지 마"로 전달. 값 없으면 자동 생략(graceful).

---

## 2. 단계별 사용성 평가

### 1단계 · 입력
- **좋음**: Lite/Pro 토글로 이미 "필수 2개 vs 전체"를 가른 건 정확한 설계.
- **고칠 점(P1)**: Pro에서 `작업유형 / 키메시지 / 시드카피 / 제약+비주얼분담 / 맥락자료`가 동급으로 길게 나열. 시드카피·제약은 이미 `<details>`라 OK이나, **키메시지·작업유형은 사용 빈도 중하** → Pro 안에서도 한 단계 더 접어 "고급"으로 묶는 걸 권장.
- **카피 일관성(P2)**: 가이드 모달 문구 "3단계면 카피가 나옵니다"는 실제 흐름(2단계=카피)과 어긋남. "2단계면 카피가 나옵니다"로 정정.

### 2단계 · 카피(루프, `sectionLoop`)
- **좋음**: 한 줄 터미널 + 펼침 로그로 과정 노이즈를 잘 숨김.
- **고칠 점**: 숨겨진 `[2] 페인·톤·전략` 섹션(264~282, `hidden`)이 루프로 통합됐는데 마크업이 남아있음 → **죽은 UI**. 유지보수 혼란. 제거 후보(JS 참조 여부 확인 필요).

### 3단계 · 기획안(`sectionCompose`) — 사용자 핵심 요청 "PPT/캔바처럼"
- **이미 되어 있는 것**(자유도): 텍스트/로고/이미지 클릭 선택, **파트별 드래그 이동**(메인/서브/CTA 독립), **우하단 핸들 크기조절**, **더블클릭 인라인 수정**, **드래그 구간 부분색**, 색/폰트/정렬/비율/CTA형태/줌. → 캔바의 "선택→이동→리사이즈→인라인편집"의 8할은 이미 구현됨.
- **중복(P0)**: 좌측 액션영역의 `이미지 생성/다시 생성`·`배경 업로드` 버튼(약 333·334행)이 이미지 패널(`blkImage`) 안의 동일 버튼(약 376·377행)과 **중복**. 좌측 큰 버튼은 "항상 노출"이라 의미 있으나, 두 곳에 같은 게 있으면 혼란. → **한쪽으로 일원화** 권장(좌측=주요 액션 1세트, 패널 안은 제거).
- **자유도 추가(P1, 캔바 근접)**: 키보드 화살표 미세이동(nudge), 드래그 시 중앙/가장자리 **스냅 가이드**, 다중요소 정렬 버튼. 이건 `_composeSelect`/드래그 핸들러에 손이 가므로 **라이브 검증 필요** → 별도 사이클 권장.

### 4단계 · 완성(`sectionFinal`) — 사용자 요청 "3단계를 기획 의도로 보고 완성도 ↑"
- **현재**: 마지막 `generateFinalBanner`는 자체 AI 루프(`SP_VISROUTE18` 비주얼 라우팅 → `runGeminiImage(imagePro)` → `SP_FINAL_JUDGE` 심사 반복)로 돌아감. **3단계 기획 패널의 편집 결과(visualData[l]: copyCarries/visualCarries/badgeCarries/layoutType/편집된 imagePrompt)와 수동 조정(composeState: 비율/색/폰트/위치)을 명시적 "기획자 의도"로 주입하지 않음** → 4단계가 3단계를 충분히 근거로 쓰지 않음.
- **권장 변경(P0, 단 라이브 검증 필요 → 미실행)**:
  1. `generateFinalBanner` 진입부에서 `var plan = visualData[l]` + `composeState` 스냅샷을 모아 **`[기획자 의도 — 반드시 존중]`** 컨텍스트 객체 구성.
  2. 비주얼 라우팅(`SP_VISROUTE18`) 입력과 `st.designPrompt`에 이 의도 블록을 prepend: "기획자가 이미 layoutType=X, 비주얼 분담=Y, 뱃지=Z, 톤=…로 결정했다. 이를 뒤집지 말고 그 위에서 완성도만 끌어올려라."
  3. 심사 루프(`SP_FINAL_JUDGE`)의 평가 기준에 "기획 의도 부합도"를 1번 항목으로 추가 → 의도 이탈 시 감점/재생성.
  - **왜 미실행**: 8개 오버레이 중 마지막 정의를 고쳐야 하고, 결과가 **유료 imagePro 생성물 품질**에 직접 영향. 라이브(API Key) 없이 출력 검증 불가 → 환경에서 같이 보며 적용하는 게 안전.

---

## 3. 중복/죽은 코드 (간소화 타깃)
| 구분 | 위치(대략) | 내용 | 조치 |
|---|---|---|---|
| 죽은 UI | 311~312 | `(구) 배너 기획안` 숨김 스텁(bannerEmpty/Loader/Msg/Cards/renderToggle) | JS 참조 확인 후 제거 |
| 죽은 UI | 264~282 | `[2] 페인·톤·전략` `hidden` 섹션(루프로 통합됨) | 제거 후보 |
| 중복 버튼 | 333·334 ↔ 376·377 | 이미지생성·배경업로드 2벌 | 1벌로 일원화 |
| 오버레이 누적 | 전역 | `generateFinalBanner`×8, `showPanel`×4, `drawComposeText`·`_catFace` 다중 | 기능 안정화 후 **마지막 정의만 남기고 정리**(별도 리팩터 사이클) |

> 오버레이 누적은 "마지막 정의가 이김" 구조라 동작엔 문제없지만, 파일 916KB·6.7k행의 상당 부분이 죽은 과거 버전. 안정화 뒤 한 번에 정리하면 체감 유지보수성이 크게 개선됨(별도 작업, 신중히).

---

## 4. 노출 우선순위 제안 (빈도 티어)
- **T1 항상 노출**: 카피(메인/서브/CTA) 수정 · 글자색 · 글자 크기 · 비율 · 이미지 생성 1버튼.
- **T2 접어두기(`details`)**: 글꼴·정렬·CTA 모양(✔ 이번에 적용) · 뱃지 누끼 · 이미지 디렉션 프롬프트 · 줌 슬라이더.
- **T3 귀퉁이/맥락 노출**: 로고 화이트 토글·로고 크기(로고 클릭 시에만 ✔ 이미 그러함) · layoutType 셀렉트.

---

## 5. 우선순위 요약
- **P0**: (3단계) 중복 버튼 일원화 · (4단계) 기획 의도 주입 — *4단계는 라이브 검증 필요해 미실행, 스펙 제공*.
- **P1**: (1단계) Pro 2차 접기 · (3단계) 스냅/넛지 등 캔바 근접 인터랙션.
- **P2**: 가이드 문구 정정 · 죽은 UI 제거 · 오버레이 정리(대형 리팩터).

> 이번 세션에서 **P0의 안전 가능분(3단계 저빈도 접기 + 3단계 이미지 의도 반영)을 실행·검증**했고, 구조·유료 출력에 영향 주는 항목은 함께 보며 적용하도록 스펙으로 남김.

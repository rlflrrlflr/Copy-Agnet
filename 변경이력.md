# 소재냥인(카피 엔진) 변경이력

## 2026-06-18 — v30 (버전 분리) 퍼포먼스·퀄 패스 [진행 중 · /loop 자율 반복]
v29 피드백 2차. 큰 변화라 **`v30_studio.html`로 버전 분리**(v29 보존, 앞으로 큰 변화마다 버전업). 라벨·localStorage 키 v30. 목표: 마케터 실사용·매체 집행 가능 퀄.

### 2차 라운드(피드백 반영 진행)
- **[1·2단계 안정화·퀄]** `S1.analyze` try/catch/finally+busy 가드로 **'분석중' 멈춤 버그 해소**(어떤 오류든 버튼 복구·진행). `Engine._json`로 추론모델 출력(코드펜스·사고텍스트) 견고 파싱 → 라이브 실패 시 조용히 시뮬로 떨어지던 문제 완화. 분석에 **USP 추출**(‘가죽’→‘나파가죽’ 등 구체 용어 그대로) 추가 → 카피 프롬프트가 USP 반영.
- **[3단계] 레이아웃 픽스(img5)**: 좌측 패널 min-width:0 → 상품/참고 드롭존 그리드 밖 튀어나감 해소. ‘이미지 생성’→‘🔄 배경 다시 생성’.
- **[3단계] 폰트**: Pretendard 전체 weight + 눈누 인기폰트 로드, **두께(굵기) 조절** UI 추가.
- **[3단계] CTA 가로/세로 독립 조절**(cpadX/cpadY), **뱃지 크기 상한↑**(200→420), **인라인 편집 겹침(img2) 해소**(_editing로 캔버스 레이어 숨김+에디터 불투명).
- **[3단계] 이미지 레이어 크기(±) 슬라이더 복원**(원본 기준 축소/확대, ver27 이전 기능).
- **[1단계] 로고 드래그앤드롭 + 악센트 자동 추출**(로고 대표색 샘플링 → 브랜드 악센트 자동).
- **[남은 라운드]** 고양이 v28 커스텀 픽셀 앞발·책상·서류 포팅(붕 뜸·이모지 제거, 픽셀퍼펙트) / 4단계 v28 로직 롤백+기획의도 패널 제거 / 1단계 좌측 장식. (둘 다 v28 원본 발췌·라이브 검증 필요 → 다음 세션 라운드)

> 이번 세션 검증: 구문 OK + 단위 PASS — `_test_analyze`(멈춤버그·USP·파싱), `_test_s2`, `_test_s1drop`, `_test_cat_v30`, `_test_models`, CTA 독립사이즈·인라인·폰트두께·로고악센트·이미지배율 각 테스트. 커밋 10건.

### 6차 라운드(본부장·CD·실무자 3각도 자가검토 → 신뢰성 개선)
실제 앱 구동 + 실제 키로 다품목 생성해보니 **치명적 신뢰성 문제** 발견: 텍스트-투-이미지 완성본이 품목/런에 따라 **안개·그라데이션(콘텐츠/글자 없음)으로 실패**(트렁크매트는 잘 나오다가도 다른 런에선 전부 안개). 단위테스트는 통과하지만 실제 산출물이 들쭉날쭉 → 팔 수 있는 툴이 아님.
- **[신뢰성] 생성 품질 게이트 + 가독 보장 폴백(핵심)**: 생성 이미지의 **엣지(선명한 경계) 비율**로 안개/빈화면 실패를 감지(`Engine._imgEdge`; 실패=edgeFrac≈0, 정상 배너=0.15). 실패 시 1회 더 강하게 재생성, 그래도 실패면 **캔버스 합성본(Design)으로 자동 폴백** → 유저는 절대 안개를 받지 않고 **항상 가독 가능한 배너**를 받음(‘다른 버전’으로 AI 재시도 가능). 프롬프트에 anti-washout(노출·대비·또렷한 글자) 가드 추가, 안개 유발 형용사(soft/breathing room) 제거.
- **[데모/무키] 시뮬 카피가 제품과 무관(소싱·마진 템플릿) 문제 수정**: 무엇을 팔든 **오퍼/USP에서 끌어낸 제품 카피**로 생성(나파가죽·IPX7 등 반영). 한글 **조사(으로/로·은/는·이/가·을/를) 받침 보정**으로 ‘매트으로’ 같은 문법 오류 해소.
- **[실무] 비율 9:16 추가**(스토리/릴스/쇼츠) — 1:1·4:5·16:9·9:16. ‘전 비율 PNG’가 완성본(AI배너)일 때 **비율마다 새로 생성**하도록 수정(기존엔 거친 오버레이로 잘못 떨어짐).
- **[정합] 4단계 ‘레이어로 열어 편집’ 정직화**: 완성본은 텍스트가 구워졌으므로, 3단계로 보낼 때 **텍스트 없는 원본 배경 + 편집 가능한 레이어**로 전환(가짜 레이어편집 X). `drawLayer` 이미지 방어 처리.

> 6차 검증: 구문 OK + `_test_gate` PASS(엣지게이트 0 vs 0.15·폴백 degraded) + 전체 리그레션 PASS. 실제 키로 다품목/9:16 생성해 실패 패턴·게이트 동작 육안 검증(현재 모델 런이 불안정해 폴백이 그 가치를 입증).
> ⚠️ 채팅 노출 Gemini 키는 폐기/재발급 권장.

### 9차 라운드(실사용 피드백 대량 반영 — 1·2·3·4단계 + 고양이)
스크린샷 기반 다발 피드백을 영역별로 반영.
- **[1단계] 검색 보강 중에도 고양이 게임**: `Engine.research` 호출을 `Cat.show/hide`로 감싸 검색 대기(20~40초)에도 가위바위보가 뜨게(지루함 방지).
- **[2단계] 부정 카피 가드**: 분석·카피 프롬프트에 ‘메인은 긍정·효익 중심, 부정/문제 묘사는 문제제기형 톤에서만·기존/경쟁 상황 한정, 제품 자체를 부정 연상시키는 표현 금지’ 규칙 추가.
- **[2단계] 대화가 메인(LLM식)**: 채팅 입력을 카드 **위 상단**으로 올리고 강조 카드화 + **깜빡이는 커서(캐럿)**·입력 펄스로 입력 유도. 카드엔 ‘✨ AI 제안 5안 — 클릭해 고르세요’ 라벨, 등장 애니메이션을 더 **쇽쇽**(바운스·스태거 0.075s).
- **[2단계] ‘새 각도로 다시’가 매번 확실히 다르게**: 누를 때마다 진입 각도를 7종(가격·시간·감성·안전·후기·경쟁우위·호기심)으로 **회전**시켜 프롬프트에 주입 → 체감 차이 큼.
- **[고양이] 승리 시 시무룩**: 이기면 **시무룩 표정을 먼저 또렷이(650ms) 보여준 뒤** 풀죽어 내려가게(기존엔 표정 없이 사라짐).
- **[3단계] 이미지 레이어 추가 해결**: 상품 썸네일 **클릭=레이어로 추가**(기대 동작)로 변경, 배경 적용은 별도 ‘배경’ 버튼. (로직은 정상이나 ‘클릭=배경’이라 레이어가 안 생기던 UX 원인 제거.)
- **[3단계] 텍스트별 개별 음영**: 메인·서브 **각 블록에 딱 붙는 타원 음영**을 따로(기존엔 전체 합집합 중심이라 글자와 어긋남). **CTA는 자체 배경이 있어 음영 제외**.
- **[3단계] 디테일컷 반영**: 상품 디테일컷을 제품 렌더에 활용 + **강조 디테일 확대 인셋** 1개 허용(제공 컷에서만, 임의 생성 금지).
- **[3·4단계] 로고 파이프라인**: 1단계 로고(누끼)를 **3단계에 자동 레이어**로 배치 → 4단계 생성 시 **로고 이미지를 그대로 첨부**하고 ‘LAST attached image=LOGO, 변형(재그림·리컬러·왜곡·자르기) 절대 금지’ 지시. 랜딩 로고가 아닌 **업로드 로고**가 변형 없이 반영.
- **[4단계] 레이아웃 LLM식**: **대화창 상단** + **완성본 옆에 후보(완성안 1·2·3)** 세로 배치(휠 안 내려도 대화·선택).
- **[4단계] 3구멍 로딩 → 다 만든 뒤 내보내기**: 생성 시작 즉시 **3개 슬롯을 스켈레톤(시머)으로** 먼저 띄우고 하나씩 채움. 완성본 캔버스도 ‘생성 중…’. **생성이 끝나야 내보내기 버튼 활성화**(다운로드 시 즉석 생성→오류 오해 방지). 전 비율 내보내기도 진행 표시(‘1/4 생성 중…’).
- **[4단계] JSON 스펙 기반 생성(동질성)**: 프롬프트로 뭉뚱그리던 것을 원본 레이어의 **구조화 JSON 스펙(요소·텍스트·존·모양)**으로 전달. **‘스펙에 있는 요소만 렌더, 임의 뱃지·씰·가격·로고·워터마크 추가 절대 금지’** 하드룰 → 원본에 없는 뱃지 남발/이탈 차단. **뱃지는 원본(doc)에서만**.
- **[4단계] 의미있는 베리에이션**: 완성안 1·2·3을 ‘카피 위치만 다름’ → **에디토리얼 미니멀 / 볼드 프로모 / 솔리드 카드 패널**로 디자인 시스템 자체를 다르게(내용·장면·요소는 동일 유지).

> 9차 검증: 구문 OK + 신규 `_test_round8`·`_test_imgclick` PASS + 전체 리그레션 PASS(`_test_newfeat`·`_test_regen`·`_test_s4`·`_test_gate`·`_test_round4`·`_test_round3`·`_test_analyze`·`_test_s3extra`). 2·3·4단계 레이아웃 스크린샷 육안 확인(대화 상단·후보 옆·3구멍 로딩·내보내기 잠금).
> ⚠️ 채팅 노출 Gemini 키는 폐기/재발급 권장.

### 8차 라운드(품질 상향 2종 — 1단계 검색 보강 + 4단계 비전 결함 점검·수정)
카피 퀄 편차/이미지 결함 문제에 대해 두 축으로 품질을 끌어올림. 둘 다 실제 키로 API 능력 검증 후 통합.
- **[1단계] 🔎 검색으로 브리프 보강(Google Search 그라운딩)**: 분석 전 옵션 체크 시 Gemini `googleSearch` 도구로 **시장·경쟁·타겟 커뮤니티 어휘·차별점·규제 주의표현**을 사실 기반 수집(`Engine._geminiResearch`/`research`). JSON 스키마와 양립이 어려워 **평문으로 받아** `App.brief.research`에 저장하고, `Engine._researchBlock()`이 **분석·카피 프롬프트 양쪽에 주입** → 더 풍부·구체적인 브리프가 2단계로 전달. 옵션이라 기본 속도 유지(켜면 20~40초↑, 무키/실패 시 조용히 기본 분석으로). 체크 상태·리서치 결과는 자동 저장/복원.
- **[4단계] 🔍 결함 점검·수정(비전 평가 모델로 수렴본 퀄업)**: 선택한 AI 완성본을 멀티모달 비전 모델이 **게재 불가급 '치명 결함'만** 진단(한글 깨짐·오타·잘림·과노출·제품 왜곡·로고 변형 등; 미적 취향 제외, JSON으로 `{ok,defects,fix}`). 결함 발견 시 **같은 완성본을 베이스로(`p.baseOverride`) 같은 구도 위에서 보정 재생성** → 엣지 게이트 통과 + **재검수(rep2)로 더 나아졌을 때만 교체**(아니면 원본 유지). 발산&수렴(생성→게이트) 뒤에 **평가→보정 루프**를 한 단계 더 얹어 마지막 한 장의 신뢰도를 높임.
  - `_geminiImageOnce`에 `baseOverride` 분기 추가(직전 완성본을 base로 재사용). 비전 JSON모드(이미지+responseMimeType) 실제 키로 200·정상 JSON 검증.

> 8차 검증: 구문 OK + `_test_newfeat` PASS(체크박스·research/critique/qcFix 존재·researchBlock 주입·collect 토글·무키/비AI 가드 안전) + 전체 리그레션 PASS(`_test_regen`·`_test_s4`·`_test_gate`·`_test_round4`·`_test_round3`·`_test_analyze`·`_test_s3extra`). 실제 키로 검색 그라운딩(34.5s·groundingMeta)·비전 결함점검(JSON 200) 라이브 검증.
> ⚠️ 채팅 노출 Gemini 키는 폐기/재발급 권장.

### 7차 라운드(GitHub 쓰기권한 확보 후 직접 push 시작 / 4단계 방향 (A) 확정)
방향 결정: **AI 이미지에 텍스트까지 굽기(A)**로 확정. GitHub App 설치(Contents: write)로 이제 파일이 아닌 **리포에 직접 push**.
- **[고양이] 흰 박스 진짜 원인 제거**: catChar `<img>`가 정체불명의 카드 배경(`--card`)+테두리(`--line`)를 갖고 있었음(매칭 규칙은 안 잡히는 모순) → `.cat-char{background:transparent;border:0;box-shadow:none}` 강제. 스프라이트는 투명 확인(마젠타 테스트). 얼굴 보이게 raise.
- **[3단계] 가독 음영 국소화**: 큰 방사형 → 텍스트 클러스터에 **딱 붙는 타원**(translate+scale)로. 이미지 전체에 안 깔림.
- **[3단계] ＋이미지 버튼**: 툴바에 추가(파일→이미지 레이어, 드래그·크기·삭제). '이미지 레이어 어딨냐' 해소.
- **[4단계 전면 정비]**:
  - 컨셉 라벨 **가격강조/타임특가/임팩트세일(마케터 셀프서빙 잔재) 제거** → **완성안 1/2/3**(같은 3단계 장면 위 3가지 디자인 마감, 톤 일관).
  - AI 실패/무키 폴백을 거친 Design 아키타입(퍼플) → **3단계 기획안 렌더(`_docPNG`)**로 교체. 이미지가 3단계 그대로 넘어오고, **하단바·음영·뱃지 반영**. 실패 시 **실제 에러 토스트로 표면화**.
  - 3단계 **CTA 하단바**를 `_finalPrompt`에 반영(full-width bottom bar).
  - **'디자인 스펙' 다운로드 제거**.

> 7차 검증: 구문 OK + `_test_s4`(하단바 프롬프트·완성안 라벨·무키 1폴백)·`_test_gate`(fallback)·전체 리그레션 PASS. 무키 폴백 렌더 육안 확인(하단바 적용). 커밋 3건 리포 push(`194155a`·`9037c8c` 등).
> ⚠️ 채팅 노출 Gemini 키는 폐기/재발급 권장.

### 5차 라운드(추가 피드백 — 누락 없이 전부 + 실제 키 검증)
- **[고양이] 흰 박스 제거 + 가시성**: 가운데 흰 종이(p3) 숨김(‘하얀테두리’ 원인), 고양이를 서류 위로 올려 **얼굴이 보이게 peek**(책상에 가려 안 보이던 것 해소). 승리 시 과하게 뜨던 것도 정리.
- **[1단계] 속도**: 분석+카피5안을 **한 번의 호출로 통합**(왕복 2→1회) → 2단계 진입 체감속도 대폭 개선. analyze 스키마/coerce/시뮬에 copies 포함, `S1.analyze`가 통합 결과 사용.
- **[3단계] CTA ‘하단바’ 실제 동작**: shape=bar면 **자동으로 화면 하단 풀폭 바**로(위치/폭 자동), 텍스트 가운데 정렬.
- **[3단계] 이미지 레이어 클릭**: 히트테스트 확인(정상) — ＋레이어로 추가한 이미지는 캔버스에서 선택·이동·크기조절 가능(테스트 통과).
- **[3·4단계] 가독 음영을 ‘글자 근처에만’**: 전체 하단 그라데이션 → **메인카피(텍스트 클러스터) 중심의 부드러운 국소 음영**(`_textCluster`+방사형). 슬라이더로 세기 조절.
- **[4단계] 3단계 이미지 재사용(구도 유지)**: 완성본 생성이 **3단계 이미지를 베이스로 받아 텍스트만 합성**(image editing) → **구도·조명·제품 배치가 그대로**. (실제 키로 base→edited 검증: 동일 구도 유지 확인.)
- **[4단계] LLM 대화창 UI로 개편**: 좌우 2열(높이 불일치) → **세로 챗 플로우**(완성본 크게 → 후보 가로 카드 → ‘말로 고치기’ 챗바 → 내보내기). ‘키없이 미리보기 디자인 셋팅’ 폴드 **완전 삭제**. ‘디자인 자유도 높게’ 토글 복원, ‘🧩 레이어로 열어 편집(3단계)’로 캔바식 레이어 분리 편집 경로 명시.
- **모델 속도 참고**: 텍스트 gemini-2.5-flash(4s)/flash-latest(8.7s)/3.1-pro(11s) 측정 — 통합호출로 pro 유지하면서 체감 개선.

> 5차 검증: 구문 OK + `_test_round4` PASS(통합분석·CTA하단바·국소음영·이미지클릭·4단계챗UI·자유도·구도유지프롬프트) + 전체 리그레션 PASS(`_test_cat_story`·`_test_s4`·`_test_s3extra`·`_test_round3`·`_test_analyze`·`_test_models`). 실제 키로 4단계 base-reuse(구도 보존) 육안 검증 완료.
> ⚠️ 채팅 노출 Gemini 키는 폐기/재발급 권장.

### 3차 라운드(피드백 반영 — 유보 항목 동시 처리)
- **[고양이] 카운트 중 ‘안보이는 구간’ 버그 해소 + 기대 연출**: `.psy`가 entrance 애니(`!important`)를 덮으며 opacity가 base(0)로 리셋돼 **세는 동안 고양이가 사라지던** 핵심 버그 수정(catPsy 키프레임에 `opacity:1` 고정). 좌우로 튀던 흔들림→가운데 유지 **기대 바운스**로 변경, **“👀 고양이가 노려본다…냥” 말풍선**(`#catThink`) 추가 — 세는 내내 노려보는 긴장감.
- **[2단계] 대화로 카피 다듬기(초보 친화)**: 카드 직접수정은 그대로 두고, 하단에 **채팅 박스**(`S2.chat`/`#s2log`) 추가. 자연어 요청(“더 짧게”·“숫자·할인 강조”·“‘매트’ 꼭 넣어”·“덜 공격적으로”)으로 5안을 다시 다듬음. Engine `refine` kind 추가(프롬프트·스키마·coerce 재사용·시뮬 결정형 변형). 제품명사·USP 보존.
- **[3단계] 이미지=레이어(크기조절·삭제)**: 상품/참고 썸네일에 **‘＋레이어’ 버튼**(크기조절·삭제 가능한 이미지 레이어로). 레이어 패널 각 행에 **🗑 삭제** 추가(숨기기와 별개). → ‘크기 슬라이더가 안 된다’던 건 이미지가 레이어가 아니라 배경이라 슬라이더가 안 뜨던 문제 → 레이어화로 해소.
- **[3단계] 배경 생성 의도 명확화**: 버튼 ‘🔄 상품컷 기반 배경 생성’ + 툴팁/토스트로 **상품컷을 합성·보존한 새 배경 장면을 생성**(상품컷 위 글씨만 얹는 PPT식이 아님)임을 명시. (엔진은 이미 product IDENTITY-LOCKED 합성 — 메시지가 불명확했음.)
- **[4단계] ‘읽어낸 기획자 의도’ 패널 제거(img1)**: 유저에겐 불필요(다음 에이전트용 정보)라는 피드백 반영. HTML `intent-box`·`#intentList` 제거, `S4.intent()` no-op(의도는 `App.doc.intent`에 보존되어 생성에만 사용). 헤더 카피도 ‘기획자 의도’ 표현 정리.
- **[브랜드] 로고 누끼(배경 자동 제거, img3)**: `Brand.knockout` — 네 모서리가 단색이면 그 색(±tol)을 투명 처리한 PNG로 변환해 박스 없이 합성. 풀블리드/사진 로고는 건드리지 않음(원본 유지). 업로드 시 자동 적용.
- **ScheduleWakeup 부재 안내**: 이 원격 환경엔 자동 재기동 도구가 없어 ‘턴 넘어 자율 반복’ 불가 → 한 턴에 몰아서 처리하는 방식으로 전환.

> 3차 검증: 구문 OK + `_test_round3` PASS(대화refine 4종·이미지레이어/삭제/배율·4단계패널제거·로고누끼·고양이말풍선) + `_test_cat_v30`·`_test_analyze`·`_test_models` 리그레션 PASS. 스크린샷으로 고양이 카운트중 가시성·2단계 채팅 육안 확인.

### 4차 라운드(유보 0 — 전부 완결, 실제 키로 이미지 검증)
- **[고양이] v28 스토리형 냥바위보 완전 포팅**: `_catStory` 스토리라인 복원(카피 작성 중 → 고양이 등장해 서류 위에 떡 → "가위바위보 이기면 내려간대!" → 3·2·1 → 동시공개). **커스텀 픽셀 앞발(`_BIGPAW` SVG)** 복원(이모지 X), **책상·서류 더미 PNG 에셋** 포팅, 고양이 승리 시 **서류 와르르(scatter)**. 승리 시 과하게 뜨던 것 완화(44→34px), 표정 스프라이트 swap(neutral/happy/sad), best 점수 저장, Engine.show/hide 진행바 연결.
- **[2단계] 순차 등장 + 안내**: 카드 ‘챠챠챡’ 순차 등장(animationDelay·cardIn). 안내문 “더 수정하고 싶다면 말씀해주세요 / 카피 클릭·더블클릭 직접 수정”.
- **[3단계] 가독성 음영 슬라이더 복원**: 자동(.45)으로 깔리되 0~80% 조절(`S3.setScrim`). 이미지=레이어(＋레이어 버튼)·🗑 삭제는 3차에서 반영. **CTA ‘하단바’ 토글을 메인 속성으로 노출**(● 알약 / ▭ 하단바).
- **[4단계] v28식 ‘완성 배너 통째 생성’으로 전환(저퀄 PPT 해소)**: 거친 캔버스 텍스트 오버레이 대신, **Nano Banana Pro로 텍스트·뱃지·CTA까지 구운 완성 배너 1장**을 통째 생성(`_finalPrompt`, 3방향: 클린·임팩트·라이프스타일). 키 없으면 텍스트 미리보기로 폴백 + 안내(빈 그라데이션 방지, strict 모드). 말로 고치기(`S4.refine`)·다른 버전(`regenBg`)도 전체 재생성. ‘기획 의도’ 패널은 3차에서 제거됨.
  - **실제 키 검증**: `gemini-3-pro-image-preview`+imageConfig(2K)로 한글 카피("미니멀 순정핏 가죽 트렁크 매트" 등)·뱃지("최대 50%")·CTA가 **정확·또렷하게 렌더**되는 완성 배너 3종(서로 다른 디자인) 생성 확인. 매체 집행 가능 퀄.
  - ⚠️ 브라우저 직접호출은 file:// 헤드리스에서 CORS로 막혀 라이브 경로는 노드로 검증(실사용 브라우저에선 3단계 배경 생성처럼 동작).
- **로고 누끼(`Brand.knockout`)는 3차에서 반영**.

> 4차 검증: 구문 OK + 단위 PASS — `_test_cat_story`(스토리·서류scatter·승패·best) / `_test_s3extra`(음영·CTA하단바·순차) / `_test_s4`(완성배너 3종·strict·프롬프트·폴백) / `_test_round3`·`_test_analyze`·`_test_models` 리그레션. 실제 키로 완성 배너 3종 육안 검증(한글 텍스트 정확).
> ⚠️ 사용자가 채팅에 노출한 Gemini 키는 폐기/재발급 권장(레포엔 커밋 안 함).

- **[최고 모델로 업그레이드]** 퀄 저하의 한 축이 저사양 모델이라는 지적 반영(비용 무관, 최고 우선).
  - 텍스트(카피·분석): `gemini-2.5-flash` → **`gemini-3.1-pro-preview`**(Gemini 3.1 Pro, 최상위 추론).
  - 이미지: `gemini-3.1-flash-image`(Nano Banana 2, 속도형) → **`gemini-3-pro-image-preview`**(Nano Banana Pro, 전문 제작용·고정밀 텍스트·스튜디오급) + **2K 해상도·비율 자동 매칭**(imageConfig).
  - Claude는 이미 최상위 `claude-opus-4-8` 유지. Claude max_tokens 1400→3000(5안 여유). 헤더 배지에 실제 모델명 표기.
- **[1단계] 드래그앤드롭 영역화 + 불필요 텍스트박스 제거**: 상품컷·레퍼런스·맥락 자료를 점선 드롭존(끌어다 놓기/클릭)으로. 맥락의 큰 textarea(피드백 img1) 제거 → 자료 파일(.txt/.md/.csv) 드롭 시 읽어서 맥락 텍스트로.
- **[2단계] 카피 5안(TOP3+보조2)·인라인 수정·제품 명사 강제**: 성과순 5안, 카드에서 메인·서브·CTA 직접 수정(→3단계/이미지 반영), 프롬프트·시뮬에 '제품 카테고리 명사 1회 이상' 강제(트렁크 매트가 카피에 드러나도록).
- **[고양이 게임 대개편]** 상단 큰 카운트/결과 배너(가위 바위 보!→승/패 색), 큰 앞발 공개(슬램), 플래시·무대 흔들 타격감, 무대 높이 150→236(여유), 카운트 중 버튼 비활성. 점수·연승·최고 보존.
- **[남음]** 3단계(인라인 겹침·이미지 리사이즈/이동·뱃지 크기·CTA 모양/크기·버튼명) / 4단계(v28 로직 롤백·기획의도패널 제거).

### 검증(헤드리스, CDN 0)
- 구문 OK. 단위 PASS: `_test_s2`(5안·TOP3·수정·명사) / `_test_s1drop`(드롭존·파일ctx) / `_test_cat_v30`(카운트·공개·타격·표정) / `_test_models`(모델ID·2K·비율).

## 2026-06-18 — v29 피드백 풀패스 (울트라 코드 모드, 13항목 정리)
사용자 13항목 피드백을 빠짐없이 실행 + 전 단계 헤드리스 검증(에러 0). 모든 변경은 `v29_studio.html` 단일 파일.

### 실행 항목
- **(13) 기본값 Gemini로 회귀**: `App.provider="gemini"`, 헤더 셀렉트/키 입력 순서도 Gemini 우선. Claude는 키 넣을 때만 사용.
- **(1·2·3) 맥락 자료 인테이크**: 1단계에 URL 랜딩·자료조사 입력 + 역할 드롭다운(랜딩/레퍼런스/경쟁사/리서치/브리프/기타). `Ctx` 모듈 — URL 자동 https 보정, 도메인 휴리스틱 **자동분류**(쿠팡=경쟁사·브런치=리서치 등), 행별 재분류. 카피 프롬프트(analyze·copies)에 `[맥락 자료]` 블록 주입. 자동 저장/복원 포함.
- **(5) 3단계 기본 폰트 프리텐다드 고정**: `Doc.text` 기본 font=Pretendard(메인·서브).
- **(7) 뱃지 폰트**: 숫자/문구 렌더를 Black Han Sans→Pretendard로.
- **(6-a) 선택 박스 정렬 버그**: 캔버스 레터박스 오프셋(getBoundingClientRect)을 반영해 박스가 글자에 정확히 정렬.
- **(6-b) 텍스트 부분색 구현**: `measure()`가 렌더문자→소스인덱스(srcIdx) 매핑 반환 → 줄바꿈/워드랩 넘어가도 올바른 글자에 색. 인라인 편집기에 **구간색 팔레트**(드래그 선택→스와치 클릭=그 구간만 색, 지우기). `applyRun/clearRun`로 겹침 분할·병합.
- **(8) Shift 다중 선택**: `App.sels` 집합. Shift 클릭 그룹 토글, 그룹 드래그(주 레이어 이동량 동일 적용), 그룹 넛지/삭제/복제, Ctrl+A 전체선택. 주 선택=실선+핸들, 보조=노란 점선.
- **(9) 3단계 이미지 자동 생성**: 진입 시 시뮬 배경 자동(키 없어도), Gemini 키 있으면 1회 자동 생성(`S3.mount`). "완전 수동" 해소.
- **(4) 고양이 가위바위보 의미 복원**: 단순 '고양이만 나옴' → 3초 룰 카운트다운+안달 모션 → 앞발 공개(VS 리빌) → 승/무/패 점수·연승·**최고기록(localStorage 보존)**, 표정(내 승=시무룩 / 고양이 승=신나서 완전 등장 / 무=기본), 연타 중복 집계 방지.
- **(10) 4단계 카피 피드백 패널 제거**: "줄 것도 없다" → 군더더기 패널/핸들러 정리.
- **(12) 디자인 퀄 저하 원인**: Gemini 이미지 모델이 아니라 **내 Design 레이아웃 패스의 헤드라인 오버플로**가 원인이었음 → `Design.layout`에 자동 맞춤 루프(헤드라인이 안전영역 안에 들어올 때까지 keySize 축소) 추가로 해결. (텍스트=gemini-2.5-flash, 이미지=gemini-3.1-flash-image)

### 검증(헤드리스, CDN 0)
- 구문 OK + 전 단계 스모크(1→2→3→4) 에러 0: 카피 3안·doc 3레이어·Pretendard·부분색·다중선택(박스4/핸들1)·완성 2안·피드백패널 제거·Gemini 기본.
- 단위 테스트 PASS: `_test_ctx`(맥락 자동분류·프롬프트·복원) / `_test_runcolor`(소스인덱스 매핑·겹침분할·팔레트·픽셀) / `_test_multisel`(그룹 토글·이동·삭제·전체선택) / `_test_cat`(점수·연승·최고·표정·busy게이팅·복원) / `_test_smoke`(풀플로우).
- 스크린샷 CD 자기리뷰: 부분색·다중선택·고양이게임 3컷.

### (11) 평가 메모 — "기존(v28) 로직이 다 훨 좋다, 4단계 UI가 깔끔"
v29는 깨끗한 재설계(무의존·검증가능·op-list 단일소스)지만 카피/디자인 **로직의 깊이**는 아직 v28 < 만큼 따라가는 중. 이번 패스로 3단계 자유도(부분색·다중선택·자동생성)와 게임을 v28 수준으로 끌어올림. v28의 실제 Gemini 프롬프트 체인(SP_*)·심사 루프를 v29 `Engine`에 추가 이식하면 로직 격차도 좁혀짐(후속).

## 2026-06-17 — v28 사용성 개선 패스 (실무자 관점)
전체 사용성 평가 후 **안전·검증 가능 범위**만 실행, 구조/유료출력 영향 항목은 `사용성_평가.md`에 스펙으로 분리.
- **[3단계 패널 간소화]** 저빈도 타이포(글꼴·정렬·CTA 모양)를 `<details>` 한 줄로 접고, 고빈도(비율·글자색·카피·글자크기)는 상단 노출 유지. ID·핸들러 전부 보존.
- **[3단계 이미지 생성 — 기획 의도 반영]** `_ce17Build`에 `[Planner intent]` 블록 추가: 기획 패널의 레이아웃 아키타입을 장면 지시로, 뱃지 의도를 "자리 비우고 이미지엔 글자 X"로 전달. 값 없으면 자동 생략.
- **검수**: `node --check`(SYNTAX OK) + jsdom 스모크(EVAL/SMOKE OK) + `_ce17Build` 단위 호출(Planner intent/아키타입/뱃지 주입 확인). 회귀 0.
- **미실행(스펙만)**: 4단계 완성 루프에 3단계 기획 의도 주입(유료 imagePro 출력 영향 → 라이브 검증 필요), 중복 버튼 일원화, 죽은 UI 제거, 오버레이 정리. 상세는 `사용성_평가.md` 참조.

## 2026-06-17 — v27 → v28

### 1. 고양이 캐릭터 전면 재설계 (★최우선, 인계서 1번)
**무엇**: 거대 base64 PNG(#catChar, ~538줄) 고양이를 제거하고, AI(나노바나나) 생성 픽셀 에셋 기반 "책상 위로 빼꼼 올라오는 가위바위보 고양이"로 교체.
- **표정 3종 + 눈 변화**: 기본(반짝 눈) / 신남=고양이 승(^^ 웃는 눈 + 벌린 입 + 반짝) / 시무룩=고양이 패(처진 눈 + 눈물 + 찡그림). 생성 표정시트(IMG_8449)에서 추출, `_catFace()`가 표정별 이미지 src 스왑.
- **가위바위보 앞발 확대 팝업**: 패 공개 시 큰 픽셀 앞발(주먹/보/가위, IMG_8453을 네이티브 14.5px 그리드로 충실 재추출)을 상단 VS 자리에 확대 표시. `_hands()`가 move키면 `_BIGPAW` SVG 렌더 + pawpop.
- **귀여운 모션**: 빼꼼 등장(오버슈트 + 스쿼시&스트레치), 둥실 바운스, 가위바위보 직전 안달(psy).
- **책상 뒤에서 빼꼼(컴포지팅 정정)**: 책상 전체(윗면·앞면)를 고양이보다 앞 레이어로 올려, 고양이가 책상 한가운데를 뚫지 않고 **책상 끝선(뒤쪽 가로선) 뒤에서** 머리만 빼꼼 올라오게 수정. 몸통과 시트 하단 컷은 책상 뒤로 가려짐.
- **신남 시 완전 등장**: 고양이 승(유저 패) 시 `.up` 클래스로 고양이를 위로 올려(완전히 올라온 상태) 웃는 표정(^^ 눈·벌린 입·볼터치)이 책상 끝선 위로 온전히 보이게. (기본=빼꼼, 신남=완전 등장의 "다른 모습")
- **무대**: 서류를 고양이 양옆 두 더미로 책상 면 위(더 뒤쪽)에 배치해 잘림 없이 보이게. 서류 와르르(scatter) 연출 보존.
- **결과 가림 해결**: 가위바위보 결과(VS 손) `z-index`를 9로 올려 항상 최상단에 표시, 양옆 서류 더미를 책상 면에 낮게 앉혀(높이 112→90, 아래로) 결과를 가리지 않게.
- **가운데 서류 정리**: 원본 PNG에서 함께 잘려 들어온 좌측 잔여 서류 조각(컬럼 0~7)을 제거하고 단일 서류만 중앙 정렬해 재추출.

**왜**: 손그림 벡터가 원본 귀여움을 못 살려, 사용자 지정대로 생성 픽셀 에셋을 충실히 키잉/추출해 통일. `_catStory`/`catRPS` 상태흐름·3초 룰·서류 와르르 보존.

**검수**: 인라인 본체 `node --check` + jsdom 스모크 + 헤드리스 크로미엄 무대 렌더(빼꼼/승/패 3컷).

### 2. 텍스트 부분색 — 드래그 구간색 (인계서 2번/보류1)
**무엇**: `drawComposeText`를 run 단위 렌더로 확장. `composeState.runs[part]=[{start,end,color}]`(소스 텍스트 인덱스). 줄바꿈/워드랩을 넘어가도 올바른 글자에 색 적용되도록 인덱스를 소스 기준으로 정합(`_srcStarts`). 인라인 편집기(`_ce17`)에 "구간색" 팔레트 추가 — 텍스트 선택 후 스와치 클릭으로 부분색 적용/해제.
**왜**: 메인/서브에서 드래그한 구간만 색 지정(기존엔 파트 전체색 + 첫 단어 강조만).

### 3. 메인/서브/CTA 독립 드래그 (인계서 3번/보류2)
**무엇**: `composeState.partPos.{key,sub,cta}={x,y}` 도입. `drawComposeText`가 파트별 위치로 그리고(앵커 재계산), `_composeSelect`가 클릭한 파트를 선택, 드래그가 해당 파트만 이동, `positionOverlay`가 파트 박스 표시.
**왜**: 세 요소를 각각 독립 위치 이동(기존엔 텍스트 블록 통째 + CTA 하단바만 독립).

> 2·3번 모두 **가산식**: 신규 상태(runs/partPos)가 없으면 기존 렌더와 픽셀 단위로 동일(커밋 전후 diff=0 검증). accent/scrim/CTA shape/베리에이션 등 v27 동작 회귀 0.

### 회귀 방지 확인 (v27에서 고친 동작 보존)
브리프 스키마/렌더(SP_BRIEF18·_brief18Render), SP_RESEARCH19, 베리에이션 재생성, _drawFitNoCrop, 뱃지 미주입, Asset 갤러리, 카피 규격(메인/서브/CTA) — 모두 미변경.

### 메모
- 원격 git push는 작업 환경 권한(프록시 403)으로 막혀 있어 로컬 커밋만 누적. 최종본은 `v28_copy_engine.html` 파일로 직접 전달.

# 소재냥인(카피 엔진) 변경이력

## 2026-06-18 — v30 (버전 분리) 퍼포먼스·퀄 패스 [진행 중 · /loop 자율 반복]
v29 피드백 2차. 큰 변화라 **`v30_studio.html`로 버전 분리**(v29 보존, 앞으로 큰 변화마다 버전업). 라벨·localStorage 키 v30. 목표: 마케터 실사용·매체 집행 가능 퀄.

### 2차 라운드(피드백 반영 진행)
- **[1·2단계 안정화·퀄]** `S1.analyze` try/catch/finally+busy 가드로 **'분석중' 멈춤 버그 해소**(어떤 오류든 버튼 복구·진행). `Engine._json`로 추론모델 출력(코드펜스·사고텍스트) 견고 파싱 → 라이브 실패 시 조용히 시뮬로 떨어지던 문제 완화. 분석에 **USP 추출**(‘가죽’→‘나파가죽’ 등 구체 용어 그대로) 추가 → 카피 프롬프트가 USP 반영.
- **[3단계] 레이아웃 픽스(img5)**: 좌측 패널 min-width:0 → 상품/참고 드롭존 그리드 밖 튀어나감 해소. ‘이미지 생성’→‘🔄 배경 다시 생성’.
- **[3단계] 폰트**: Pretendard 전체 weight + 눈누 인기폰트 로드, **두께(굵기) 조절** UI 추가.
- **[3단계] CTA 가로/세로 독립 조절**(cpadX/cpadY), **뱃지 크기 상한↑**(200→420), **인라인 편집 겹침(img2) 해소**(_editing로 캔버스 레이어 숨김+에디터 불투명).
- **[3단계] 이미지 레이어 크기(±) 슬라이더 복원**(원본 기준 축소/확대, ver27 이전 기능).
- **[1단계] 로고 드래그앤드롭 + 악센트 자동 추출**(로고 대표색 샘플링 → 브랜드 악센트 자동).
- **[남은 라운드]** 고양이 v28 커스텀 픽셀 앞발·책상·서류 포팅(붕 뜸·이모지 제거, 픽셀퍼펙트) / 4단계 v28 로직 롤백+기획의도 패널 제거 / 1단계 좌측 장식. (둘 다 v28 원본 발췌·라이브 검증 필요 → 다음 세션 라운드)

> 이번 세션 검증: 구문 OK + 단위 PASS — `_test_analyze`(멈춤버그·USP·파싱), `_test_s2`, `_test_s1drop`, `_test_cat_v30`, `_test_models`, CTA 독립사이즈·인라인·폰트두께·로고악센트·이미지배율 각 테스트. 커밋 10건.

- **[최고 모델로 업그레이드]** 퀄 저하의 한 축이 저사양 모델이라는 지적 반영(비용 무관, 최고 우선).
  - 텍스트(카피·분석): `gemini-2.5-flash` → **`gemini-3.1-pro-preview`**(Gemini 3.1 Pro, 최상위 추론).
  - 이미지: `gemini-3.1-flash-image`(Nano Banana 2, 속도형) → **`gemini-3-pro-image-preview`**(Nano Banana Pro, 전문 제작용·고정밀 텍스트·스튜디오급) + **2K 해상도·비율 자동 매칭**(imageConfig).
  - Claude는 이미 최상위 `claude-opus-4-8` 유지. Claude max_tokens 1400→3000(5안 여유). 헤더 배지에 실제 모델명 표기.
- **[1단계] 드래그앤드롭 영역화 + 불필요 텍스트박스 제거**: 상품컷·레퍼런스·맥락 자료를 점선 드롭존(끌어다 놓기/클릭)으로. 맥락의 큰 textarea(피드백 img1) 제거 → 자료 파일(.txt/.md/.csv) 드롭 시 읽어서 맥락 텍스트로.
- **[2단계] 카피 5안(TOP3+보조2)·인라인 수정·제품 명사 강제**: 성과순 5안, 카드에서 메인·서브·CTA 직접 수정(→3단계/이미지 반영), 프롬프트·시뮬에 '제품 카테고리 명사 1회 이상' 강제(트렁크 매트가 카피에 드러나도록).
- **[고양이 게임 대개편]** 상단 큰 카운트/결과 배너(가위 바위 보!→승/패 색), 큰 앞발 공개(슬램), 플래시·무대 흔들 타격감, 무대 높이 150→236(여유), 카운트 중 버튼 비활성. 점수·연승·최고 보존.
- **[남음]** 3단계(인라인 겹침·이미지 리사이즈/이동·뱃지 크기·CTA 모양/크기·버튼명) / 4단계(v28 로직 롤백·기획의도패널 제거).

### 검증(헤드리스, CDN 0)
- 구문 OK. 단위 PASS: `_test_s2`(5안·TOP3·수정·명사) / `_test_s1drop`(드롭존·파일ctx) / `_test_cat_v30`(카운트·공개·타격·표정) / `_test_models`(모델ID·2K·비율).

## 2026-06-18 — v29 피드백 풀패스 (울트라 코드 모드, 13항목 정리)
사용자 13항목 피드백을 빠짐없이 실행 + 전 단계 헤드리스 검증(에러 0). 모든 변경은 `v29_studio.html` 단일 파일.

### 실행 항목
- **(13) 기본값 Gemini로 회귀**: `App.provider="gemini"`, 헤더 셀렉트/키 입력 순서도 Gemini 우선. Claude는 키 넣을 때만 사용.
- **(1·2·3) 맥락 자료 인테이크**: 1단계에 URL 랜딩·자료조사 입력 + 역할 드롭다운(랜딩/레퍼런스/경쟁사/리서치/브리프/기타). `Ctx` 모듈 — URL 자동 https 보정, 도메인 휴리스틱 **자동분류**(쿠팡=경쟁사·브런치=리서치 등), 행별 재분류. 카피 프롬프트(analyze·copies)에 `[맥락 자료]` 블록 주입. 자동 저장/복원 포함.
- **(5) 3단계 기본 폰트 프리텐다드 고정**: `Doc.text` 기본 font=Pretendard(메인·서브).
- **(7) 뱃지 폰트**: 숫자/문구 렌더를 Black Han Sans→Pretendard로.
- **(6-a) 선택 박스 정렬 버그**: 캔버스 레터박스 오프셋(getBoundingClientRect)을 반영해 박스가 글자에 정확히 정렬.
- **(6-b) 텍스트 부분색 구현**: `measure()`가 렌더문자→소스인덱스(srcIdx) 매핑 반환 → 줄바꿈/워드랩 넘어가도 올바른 글자에 색. 인라인 편집기에 **구간색 팔레트**(드래그 선택→스와치 클릭=그 구간만 색, 지우기). `applyRun/clearRun`로 겹침 분할·병합.
- **(8) Shift 다중 선택**: `App.sels` 집합. Shift 클릭 그룹 토글, 그룹 드래그(주 레이어 이동량 동일 적용), 그룹 넛지/삭제/복제, Ctrl+A 전체선택. 주 선택=실선+핸들, 보조=노란 점선.
- **(9) 3단계 이미지 자동 생성**: 진입 시 시뮬 배경 자동(키 없어도), Gemini 키 있으면 1회 자동 생성(`S3.mount`). "완전 수동" 해소.
- **(4) 고양이 가위바위보 의미 복원**: 단순 '고양이만 나옴' → 3초 룰 카운트다운+안달 모션 → 앞발 공개(VS 리빌) → 승/무/패 점수·연승·**최고기록(localStorage 보존)**, 표정(내 승=시무룩 / 고양이 승=신나서 완전 등장 / 무=기본), 연타 중복 집계 방지.
- **(10) 4단계 카피 피드백 패널 제거**: "줄 것도 없다" → 군더더기 패널/핸들러 정리.
- **(12) 디자인 퀄 저하 원인**: Gemini 이미지 모델이 아니라 **내 Design 레이아웃 패스의 헤드라인 오버플로**가 원인이었음 → `Design.layout`에 자동 맞춤 루프(헤드라인이 안전영역 안에 들어올 때까지 keySize 축소) 추가로 해결. (텍스트=gemini-2.5-flash, 이미지=gemini-3.1-flash-image)

### 검증(헤드리스, CDN 0)
- 구문 OK + 전 단계 스모크(1→2→3→4) 에러 0: 카피 3안·doc 3레이어·Pretendard·부분색·다중선택(박스4/핸들1)·완성 2안·피드백패널 제거·Gemini 기본.
- 단위 테스트 PASS: `_test_ctx`(맥락 자동분류·프롬프트·복원) / `_test_runcolor`(소스인덱스 매핑·겹침분할·팔레트·픽셀) / `_test_multisel`(그룹 토글·이동·삭제·전체선택) / `_test_cat`(점수·연승·최고·표정·busy게이팅·복원) / `_test_smoke`(풀플로우).
- 스크린샷 CD 자기리뷰: 부분색·다중선택·고양이게임 3컷.

### (11) 평가 메모 — "기존(v28) 로직이 다 훨 좋다, 4단계 UI가 깔끔"
v29는 깨끗한 재설계(무의존·검증가능·op-list 단일소스)지만 카피/디자인 **로직의 깊이**는 아직 v28 < 만큼 따라가는 중. 이번 패스로 3단계 자유도(부분색·다중선택·자동생성)와 게임을 v28 수준으로 끌어올림. v28의 실제 Gemini 프롬프트 체인(SP_*)·심사 루프를 v29 `Engine`에 추가 이식하면 로직 격차도 좁혀짐(후속).

## 2026-06-17 — v28 사용성 개선 패스 (실무자 관점)
전체 사용성 평가 후 **안전·검증 가능 범위**만 실행, 구조/유료출력 영향 항목은 `사용성_평가.md`에 스펙으로 분리.
- **[3단계 패널 간소화]** 저빈도 타이포(글꼴·정렬·CTA 모양)를 `<details>` 한 줄로 접고, 고빈도(비율·글자색·카피·글자크기)는 상단 노출 유지. ID·핸들러 전부 보존.
- **[3단계 이미지 생성 — 기획 의도 반영]** `_ce17Build`에 `[Planner intent]` 블록 추가: 기획 패널의 레이아웃 아키타입을 장면 지시로, 뱃지 의도를 "자리 비우고 이미지엔 글자 X"로 전달. 값 없으면 자동 생략.
- **검수**: `node --check`(SYNTAX OK) + jsdom 스모크(EVAL/SMOKE OK) + `_ce17Build` 단위 호출(Planner intent/아키타입/뱃지 주입 확인). 회귀 0.
- **미실행(스펙만)**: 4단계 완성 루프에 3단계 기획 의도 주입(유료 imagePro 출력 영향 → 라이브 검증 필요), 중복 버튼 일원화, 죽은 UI 제거, 오버레이 정리. 상세는 `사용성_평가.md` 참조.

## 2026-06-17 — v27 → v28

### 1. 고양이 캐릭터 전면 재설계 (★최우선, 인계서 1번)
**무엇**: 거대 base64 PNG(#catChar, ~538줄) 고양이를 제거하고, AI(나노바나나) 생성 픽셀 에셋 기반 "책상 위로 빼꼼 올라오는 가위바위보 고양이"로 교체.
- **표정 3종 + 눈 변화**: 기본(반짝 눈) / 신남=고양이 승(^^ 웃는 눈 + 벌린 입 + 반짝) / 시무룩=고양이 패(처진 눈 + 눈물 + 찡그림). 생성 표정시트(IMG_8449)에서 추출, `_catFace()`가 표정별 이미지 src 스왑.
- **가위바위보 앞발 확대 팝업**: 패 공개 시 큰 픽셀 앞발(주먹/보/가위, IMG_8453을 네이티브 14.5px 그리드로 충실 재추출)을 상단 VS 자리에 확대 표시. `_hands()`가 move키면 `_BIGPAW` SVG 렌더 + pawpop.
- **귀여운 모션**: 빼꼼 등장(오버슈트 + 스쿼시&스트레치), 둥실 바운스, 가위바위보 직전 안달(psy).
- **책상 뒤에서 빼꼼(컴포지팅 정정)**: 책상 전체(윗면·앞면)를 고양이보다 앞 레이어로 올려, 고양이가 책상 한가운데를 뚫지 않고 **책상 끝선(뒤쪽 가로선) 뒤에서** 머리만 빼꼼 올라오게 수정. 몸통과 시트 하단 컷은 책상 뒤로 가려짐.
- **신남 시 완전 등장**: 고양이 승(유저 패) 시 `.up` 클래스로 고양이를 위로 올려(완전히 올라온 상태) 웃는 표정(^^ 눈·벌린 입·볼터치)이 책상 끝선 위로 온전히 보이게. (기본=빼꼼, 신남=완전 등장의 "다른 모습")
- **무대**: 서류를 고양이 양옆 두 더미로 책상 면 위(더 뒤쪽)에 배치해 잘림 없이 보이게. 서류 와르르(scatter) 연출 보존.
- **결과 가림 해결**: 가위바위보 결과(VS 손) `z-index`를 9로 올려 항상 최상단에 표시, 양옆 서류 더미를 책상 면에 낮게 앉혀(높이 112→90, 아래로) 결과를 가리지 않게.
- **가운데 서류 정리**: 원본 PNG에서 함께 잘려 들어온 좌측 잔여 서류 조각(컬럼 0~7)을 제거하고 단일 서류만 중앙 정렬해 재추출.

**왜**: 손그림 벡터가 원본 귀여움을 못 살려, 사용자 지정대로 생성 픽셀 에셋을 충실히 키잉/추출해 통일. `_catStory`/`catRPS` 상태흐름·3초 룰·서류 와르르 보존.

**검수**: 인라인 본체 `node --check` + jsdom 스모크 + 헤드리스 크로미엄 무대 렌더(빼꼼/승/패 3컷).

### 2. 텍스트 부분색 — 드래그 구간색 (인계서 2번/보류1)
**무엇**: `drawComposeText`를 run 단위 렌더로 확장. `composeState.runs[part]=[{start,end,color}]`(소스 텍스트 인덱스). 줄바꿈/워드랩을 넘어가도 올바른 글자에 색 적용되도록 인덱스를 소스 기준으로 정합(`_srcStarts`). 인라인 편집기(`_ce17`)에 "구간색" 팔레트 추가 — 텍스트 선택 후 스와치 클릭으로 부분색 적용/해제.
**왜**: 메인/서브에서 드래그한 구간만 색 지정(기존엔 파트 전체색 + 첫 단어 강조만).

### 3. 메인/서브/CTA 독립 드래그 (인계서 3번/보류2)
**무엇**: `composeState.partPos.{key,sub,cta}={x,y}` 도입. `drawComposeText`가 파트별 위치로 그리고(앵커 재계산), `_composeSelect`가 클릭한 파트를 선택, 드래그가 해당 파트만 이동, `positionOverlay`가 파트 박스 표시.
**왜**: 세 요소를 각각 독립 위치 이동(기존엔 텍스트 블록 통째 + CTA 하단바만 독립).

> 2·3번 모두 **가산식**: 신규 상태(runs/partPos)가 없으면 기존 렌더와 픽셀 단위로 동일(커밋 전후 diff=0 검증). accent/scrim/CTA shape/베리에이션 등 v27 동작 회귀 0.

### 회귀 방지 확인 (v27에서 고친 동작 보존)
브리프 스키마/렌더(SP_BRIEF18·_brief18Render), SP_RESEARCH19, 베리에이션 재생성, _drawFitNoCrop, 뱃지 미주입, Asset 갤러리, 카피 규격(메인/서브/CTA) — 모두 미변경.

### 메모
- 원격 git push는 작업 환경 권한(프록시 403)으로 막혀 있어 로컬 커밋만 누적. 최종본은 `v28_copy_engine.html` 파일로 직접 전달.
